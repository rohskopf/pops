main.py is the main code - just edit it accordingly and do:
	python main.py

Before running: Unpack the /data file:
    tar -xvzf data.tar.gz

The code now searches for EVERY .xml file inside this directory, regardless of what folder it is in.
